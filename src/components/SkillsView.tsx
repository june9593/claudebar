// Stub — Task 9 (operator panel) will rewrite this
export function SkillsView() {
  return <div style={{ padding: 16, color: 'var(--color-text-secondary)', fontSize: 13 }}>Skills view — coming in Phase 3.</div>;
}
