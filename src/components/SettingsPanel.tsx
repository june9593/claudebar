// Stub — Task 9 (operator panel) will rewrite this with ClaudeBar-specific settings
export function SettingsPanel() {
  return <div style={{ padding: 16, color: 'var(--color-text-secondary)', fontSize: 13 }}>Settings — coming in Phase 3.</div>;
}
